#include <stdio.h>

int main() {
	int a = 2; //초기화 (선언과 동시에 대입)
	int b = 3;

	printf("%d + %d = %d\n", a, b, a + b); //각각 a = 2 , b = 3 

	// 변수 이름 : 알파벳 대소문자 가능, _, 숫자(첫번째 글자 X)
	/*int abc;
	int 2a; //불가능
	int a2;
	int _under_score;
	int _;*/
}