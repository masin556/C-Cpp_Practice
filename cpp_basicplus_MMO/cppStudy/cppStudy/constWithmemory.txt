(1 << 3) // 하드 코딩 되었다는 의미 (별로 좋지 않다.)

새로 추가되는 것이 있다면 어질러 진다. 
자체의 상수값으로 만드는 것 

바뀌지 않을 변수에 대해 대문자로 함으로 코딩컨벤션을 지키는 팀들이 있다. 

누군가 엉뚱한 실수로 이어질 수 있기에 안정성을 보장해주는 경우가 많다.
한번 셋팅해서 바뀌지 않을 것이라면 
const 를 붙여줘서 못바꾸게 한다. 

변수만 살짝 바꾸면 모든 것을 그대로 적용가능하다. 
const를 붙일때 초기값이 없으면 에러가 뜬다. 
이렇게 변수를 상수화 한다고 한다.

일반적으로 할때 
main 안쪽에 하는데 

체계적으로 다가가기 위함.?
Assembly에서 전역에 하면 bss, data영역 에 하는것

main함수 - 프로시저(어셈블리) : 비슷한 기능을 한번에 묶어서 편하게 호출 할 수 있도록 하는 것



데이터 섹션 
빌드가 완료되고 실행파일이 만들어지고 실행파일이 메모리에 올라가고 메모리에 이런저런 공간이 생기며, 
Stack 메모리 
Data 영역 
	- bss
	- initialized data
	- .read only data 영역


함수 내부에 하면 data가 다른 곳에 할당 된다. 

스택영역이 복잡해서 해당 부분을 설명하지 않고, 지역변수로 잘 사용하지 않는다?
